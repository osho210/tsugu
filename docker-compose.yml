services:
  postgres:
    image: postgres:18.6
    environment:
      POSTGRES_USER: tsugu
      POSTGRES_PASSWORD: tsugu
      POSTGRES_DB: tsugu
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql

volumes:
  postgres-data: